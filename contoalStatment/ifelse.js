let age = 16;

if (age >= 18) {
    console.log("Eligible to vote");
} else {
    console.log("Not eligible to vote");
}

// Output: Not eligible to vote
// Note: if-else statement me agar condition false hoti hai to else block ka code execute hota hai.
// Hindi Explanation:
 /* if-else statement ek conditional statement hai jo do alag-alag code blocks me se ek ko execute karta hai,
  condition ke true ya false hone par depend karta hai. */
// Syntax: if (condition) { // code if true } else { // code if false }
// Agar condition true hoti hai to if block ka code execute hota hai, warna else block ka code execute hota hai.

// Condition ke andar koi bhi expression ho sakta hai jo boolean value (true/false) return kare.
// Agar condition true hoti hai to if block ke andar ka code execute hota hai, warna else block ke andar ka code execute hota hai.
// Note: if-else statement me dono blocks (if aur else) me se sirf ek hi block execute hota hai.
// 